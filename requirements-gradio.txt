# Gradio Demo Requirements
# Core dependencies for HITL system + Gradio interface

# Gradio for web interface
gradio>=4.0.0

# Core HITL system dependencies (from pyproject.toml)
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langgraph>=0.2.0
langsmith>=0.1.0
anthropic>=0.35.0
openai>=1.0.0

# Database and data handling
sqlalchemy>=2.0.0
sqlite3  # Built-in to Python
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Async support
asyncio  # Built-in to Python
aiofiles>=24.1.0

# Configuration and environment
python-dotenv>=1.0.0
pyyaml>=6.0.0
toml>=0.10.0

# Logging and monitoring
structlog>=24.0.0

# Utilities
uuid  # Built-in to Python
datetime  # Built-in to Python
concurrent.futures  # Built-in to Python
typing-extensions>=4.0.0

# Optional: For enhanced deployment
uvicorn>=0.20.0  # ASGI server
fastapi>=0.100.0  # API framework (used by Gradio)

# Development/Testing (optional)
pytest>=7.0.0
pytest-asyncio>=0.21.0